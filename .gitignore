# Node modules
node_modules/
package-lock.json

# Next.js build output
.next/
out/

# Production build
build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS-specific files
.DS_Store
Thumbs.db

# Vercel deployment
.vercel/

# Environment variables (secrets — jamais suivis par Git ; voir .env.example
# pour la liste des noms attendus). GATE SEC-1.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

# Supabase CLI local cache (project-ref, pooler info — spécifique à la machine)
supabase/.temp/

# Optional: if using TypeScript
*.tsbuildinfo

# Optional: if using testing tools
coverage/
jest-test-results.json

# Optional: if using IDEs
.vscode/
.idea/

# Optional: if using TurboRepo or monorepo
turbo/